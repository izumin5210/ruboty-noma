require 'spec_helper'

describe Ruboty::Noma do
  it 'has a version number' do
    expect(Ruboty::Noma::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
